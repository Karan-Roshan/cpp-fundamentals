// cin is used to get the user input
// cin is a predefined variable that reads data from the keyboard with the extraction operator (>>)

#include <iostream>
using namespace std;

int main()
{
    string name;
    cout << "Type your full name: ";
    cin >> name;
    cout << name;
    return 0;
}